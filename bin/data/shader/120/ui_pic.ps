#version 120

varying vec4 out_position;
varying vec2 out_texcoord;

uniform sampler2D in_texture;
uniform vec4 in_color;

void main()
{
	vec2 iv = out_texcoord;
	iv.y = 1 - iv.y;
	gl_FragColor = texture2D(in_texture, iv);
	gl_FragColor = in_color*gl_FragColor;
}